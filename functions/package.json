{
  "name": "novo-app-functions",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "lint": "eslint --ext .js,.ts . || true",
    "build": "echo 'No build needed'",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
  "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.6.0"
  }
}
