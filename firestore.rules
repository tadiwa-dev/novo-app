rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Temporary permissive rules for testing - will be updated once Anonymous Auth is enabled
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
